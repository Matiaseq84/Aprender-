class Enrollment {
    constructor(id, studentId, courseId, enrollmentId) {
        this.id = id,
        this.studentId = studentId,
        this.courseId = courseId,
        this.enrollmentId = enrollmentId
    }
}

export default Enrollment