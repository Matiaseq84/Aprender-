class Attendance {
    constructor(id, studentId, courseId, date, present) {
        this.id = id,
        this.studentId = studentId,
        this.courseId = courseId,
        this.date = date,
        this.present = present
    }
}

export default Attendance